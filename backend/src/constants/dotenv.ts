import { dotenv } from "@/config/dotenv.js";

export const NODE_ENV = dotenv.NODE_ENV;
export const PORT = dotenv.PORT;
export const DATABASE_URL = dotenv.DATABASE_URL;
export const DATABASE_USER = dotenv.DATABASE_USER;
export const DATABASE_PASSWORD = dotenv.DATABASE_PASSWORD;
export const DATABASE_HOST = dotenv.DATABASE_HOST;
